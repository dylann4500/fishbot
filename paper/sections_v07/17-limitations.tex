\section{Threats to validity, and the weaknesses that remain}
\label{sec:limitations}

\paragraph{The release adds nothing measurable to what phase~2 already had.}
This is the sharpest limitation and it is measured, not conjectured:
\vsevenBtwoComposite{}~points $[\vsevenBtwoCompositeLo, \vsevenBtwoCompositeHi]$ against \composite{}
(\S\ref{sec:against-composite}). Everything else in this paper describes a configuration whose strength
advantage over the \vsix{} frontier is real and was found in phase~2 by an unfitted coordinate sweep.

\paragraph{The gain may be concentrated in one coordinate, and the location test does not
replicate.}
\texttt{r12=25} carries it, with a leave-one-out drop of \vsevenLooRtwelve{}
$[\vsevenLooRtwelveLo, \vsevenLooRtwelveHi]$. But the protocol's own one-third location test is
\vsevenLocVerdictOne{} on one bank and \vsevenLocVerdictThree{} on the other
(\S\ref{sec:against-location}). A configuration whose gain is concentrated in a single coordinate is
also a configuration with a single point of failure, and this battery does not resolve how concentrated
it is.

\paragraph{The worst-case improvement and the minimax regret point in opposite directions.}
\frozen{} has a far better worst cell than \incumbent{} but not the best of the four ---
\composite{} is ahead on both readings --- and \frozen{} is \vsevenRegretFrozenRank{}rd of four on
minimax regret (\S\ref{sec:results-panel}). Its worst cell does not even replicate in sign
(\vsevenWorstFrozenBankOne{} / \vsevenWorstFrozenBankTwo{}, at different members). The regret difference is bought against far archetypes that
every arm already beats by \vsevenFarSpanLo{} to \vsevenFarSpanHi{} points, which is a reason to
weigh it less --- but it
is a reason, not a dismissal, and a reader who cares about regret over this panel should prefer
\fcheap{}.

\paragraph{Exploitability is a lower bound within a searched class.}
\label{sec:limitations-lbr}
\S\ref{sec:exploit} fits eight responders from named classes at a stated budget. A stronger response
outside those classes, or the same classes at a larger budget, would give a larger number. A response
that fails to reach the target proves nothing about the target; it constrains the search. The white-box
arm in particular runs \vsevenZeightLive{} live coordinates at \vsevenBfourGens{} generations of
\vsevenBfourPop{}
(\S\ref{sec:exploit-whitebox}). This is the reading error the corpus has made before and it is named
here rather than risked.

\paragraph{The opponent panel is a modelling choice and its deception archetypes are hand-built.}
The withholder, the silent and the feint are stylised implementations of manoeuvres the project owner
reported from live play. They are not people. A gain against one of them is evidence about robustness
to a class of behaviour, not a measurement of play against a person. The partner columns have the same
limitation from the other side: a ``foreign partner'' here is a weaker or deceptive bot.

\paragraph{The advantage transfers across partners less well than the incumbent's did, on the ratio
the protocol chose in advance.}
\vsevenSoneRatio{} against \vsevenBaseRatio{} (\S\ref{sec:results-partners}). The collapse test passes
and the absolute changed-partner median is much larger than the incumbent's, but the pre-committed
sentence is the one this paper uses, and it is not ``it transfers''.

\paragraph{A named engine defect is present in every number here.}
Agents are reused across deals within a thread and some per-agent state survives reset, reachable only
under the search. The carrier is named --- \texttt{\vsevenSsixCarrier{}} --- the member is not, the
gate condition removes it by rebuilding agents per deal, and \emph{that mode is not the shipped mode}.
\S\ref{sec:results-residual} shows the audited denominators moving between the two conditions, which is
the defect made visible rather than argued about. Every strength number in this paper is measured
under it.

\paragraph{Four of the seven sealed banks were digest-verified and never played as deal banks.}
The fitting driver derives its own per-generation deal stream from its seed rather than constructing
the named bank's deals, so \texttt{7090004}, \texttt{7090005} and \texttt{7091002} are verified and
then unused as deals. \texttt{7091001} is unused for a different reason: the sealed panel members
were scored on the two primary banks with the rest of the panel, so that minimax regret is computed
over one shared pair. What the protocol relies on survives --- the fitting streams are mutually
disjoint and disjoint from the evaluation banks --- but the material accounting here should be read
as ``three evaluation banks played (\texttt{7090001} and \texttt{7090002} for most cells,
\texttt{7090003} for the lattice and the dialects) plus a derived fitting stream'', not ``seven
banks played''.

\paragraph{One dialect row measures nothing, and the residual it feeds is correspondingly weaker.}
\texttt{--maxasks=360} is bit-identical to the default, so the legacy residual is legacy minus two
effective components rather than three, and its interval is \vsevenLegacyResidualRatio$\times$ the size
of the residual itself (\S\ref{sec:results-dialects}). The data are equally consistent with the
forced-endgame willingness ladder contributing nothing at all.

\paragraph{The negative results are null at this power and against this panel.}
Four architectures are reported as worth nothing (\S\ref{sec:rejected}). Each was measured paired, on
two disjoint banks, against a multi-opponent panel. That resolves effects of roughly a point; it does
not resolve effects of a quarter of a point. What this cycle establishes is that none of the
architectures on the inherited register is large --- not that the sum of many small ones is zero.

\paragraph{Cross-play cannot see a convention inherited from the shared ancestor.}
The three independently-trained runs of \S\ref{sec:results-crossplay} all start from or near the
\vsix{} vector. A convention that all three inherited from that common ancestor would appear on the
diagonal and the off-diagonal alike and would be invisible to this test. What the test rules out is a
convention formed \emph{during} each run's own fit.

\paragraph{Termination remains empirical.}
No configuration here hits the action cap in any scored cell but one --- \vsevenLimitCellArm{}
against \texttt{\vsevenLimitCellOpp{}}, one game in \vsevenLimitCellGames{} --- and the
stall rung's separation between ordinary play and a deadlock is a measurement over
\vsevenStallMirrorGames{} mirror games rather than a theorem.
What is not proved is that the stall rule plus the stopping rule terminates on every reachable
position.
